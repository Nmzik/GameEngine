#pragma once
#include <cstdint>
/*#include "opengl.h"

struct grcTexture;
class memstream;

class GLTexture
{
    GLuint textureID;
    uint32_t TextureHash;

public:
    GLTexture(grcTexture* GLTexture, memstream& file, uint32_t TextureHash);
    ~GLTexture();

    GLTexture(const GLTexture&) = delete;
    GLTexture(GLTexture&&) = delete;
    GLTexture& operator=(const GLTexture&) = delete;
    GLTexture& operator=(GLTexture&&) = delete;

    GLuint getTextureID() const
    {
        return textureID;
    }
};*/
